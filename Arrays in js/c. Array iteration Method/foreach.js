// The forEach method executes a provided function once for each array element.


// Syntax : array.forEach(i => {});

// ex:

const array = [1, 2, 3, 4, 5];

array.forEach(element => {
  console.log(element);
});


